{
  "name": "limovatest-v2",
  "private": true,
  "version": "0.1.0",
  "description": "Simple Firebase Hosting and Functions test app for Limova API experiments.",
  "scripts": {
    "install:functions": "npm install --prefix functions",
    "firebase": "npx --yes --package firebase-tools firebase",
    "login": "npm run firebase -- login",
    "projects": "npm run firebase -- projects:list",
    "use": "npm run firebase -- use --add",
    "emulators": "npm run firebase -- emulators:start --config firebase.json",
    "deploy": "npm run firebase -- deploy --config firebase.json --only hosting,functions",
    "deploy:backend": "npm run firebase -- deploy --config firebase.json --only functions",
    "deploy:frontend": "npm run firebase -- deploy --config firebase.json --only hosting"
  }
}
